name: test checkout code

on: 
  push:
    branches:
      - main
jobs:
  retyping:
    runs-on: ubuntu-latest
    steps:
      - name: start chekout
        run: echo "start chekinh out"

      - name: checking-out
        uses: actions/checkout@v6.0.2

      - name: chekout finished
        run:  ls -la
